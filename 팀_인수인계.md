# 인게임 시스템 구현 내역 (hyeona-feat)

> 작업 브랜치: `hyeona-feat` · 커밋 16개 · 스크립트 56개 파일, 약 7,900줄 추가
> Claude용 작업 규칙은 [`CLAUDE.md`](CLAUDE.md), CSV 작성법은 [`CSV_가이드.md`](CSV_가이드.md)

---

## 먼저 알아야 할 것

### 게임을 실행하려면

1. Unity Hub에서 **`2KH1-2KH1project`** 폴더를 연다 (⚠️ `My project` 아님)
2. `Assets/Scenes/`**`Title.unity`**를 열고 재생 — 반드시 Title부터 시작할 것
3. 그림을 새로 넣었다면 상단 메뉴 **`[2KH1] → [Illusts 폴더 그림 임포트 설정 다시 적용]`** 한 번 실행

### 드라이브에서 받아 넣어야 하는 것

`Assets/Resources/` 폴더는 git으로 공유되지 않는다. 아래를 직접 넣어야 게임이 돈다.

```
Assets/Resources/
├─ Dialogues/   CSV 8종 (아래 표 참고)
├─ Illusts/
│  ├─ Backgrounds/   BG_*.png   34장
│  ├─ Objects/       OBJ_*.png  47장
│  └─ Standings/     STD_*.png  58장
└─ Sounds/      BGM + SFX/
```

---

## 새로 만든 기능

### 1. 일러스트 시스템

배경·캐릭터·조사 오브젝트를 **파일 이름으로** 불러온다. 인스펙터 연결이 필요 없다.

- **배경**: CSV의 `Background` 칸에 파일명만 적으면 바뀐다. **빈 칸이면 이전 배경 유지**
- **캐릭터 스탠딩**: 최대 3명(왼쪽/가운데/오른쪽). `Standing` 칸이 빈 줄은 표정이 유지되므로
  **바뀌는 줄에만** 적으면 된다
- **립싱크**: 파일명 뒤에 `_OpenMouse`가 붙은 짝이 있으면 대사 타이핑 중 자동으로 입이 움직인다.
  누가 말하는지는 `Characters.csv`의 한글이름 ↔ 영문토큰 표로 판별
- **투명 픽셀 클릭 무시**: 조사 오브젝트는 그림이 그려진 부분만 눌린다. 자동 적용

### 2. 조사 시스템 (전면 재작성)

**배경 그림 위에 놓인 오브젝트를 직접 클릭**하는 방식이다. 검은 창이나 placeholder는 없앴다.

- 조사 결과는 **대화창에 출력**된다. 서류·사진처럼 자료 자체를 읽어야 하는 것만 전체화면 뷰어
- 조사 화면은 CSV만 보고 **런타임에 생성**된다. 씬에 만들어둘 것이 전혀 없다
- `InvestigationId`는 **배경 파일명과 1:1** (`BG_01_MyDesk`). 배경 34장 전부가 조사 화면
- 조사 오브젝트 47개 전부 각 배경에 배치 완료

### 3. 대화 시스템

| 기능 | 내용 |
|---|---|
| 타이핑 | 4단계 속도 (느리게/보통/빠르게/즉시) |
| 쪽 나누기 | 긴 대사가 창을 넘으면 자동으로 다음 쪽. 클릭하면 넘어감 |
| 즉시 완성 | 타이핑 중 클릭 = 다음 줄이 아니라 현재 줄 즉시 완성 |
| 자동 진행 | 옵션에서 켜면 저절로 넘어감. 대기시간 조절 가능 |
| 화자 강조 | 이름을 굵게 + 금색, 지문일 땐 이름 칸 숨김 |
| 계속 표시 | 오른쪽 아래 ▼ 깜빡임 |

### 4. 아이템 / 가방 (Inven 탭)

- 획득한 아이템이 목록에 쌓이고, 누르면 이름·설명·그림이 나온다
- **조합**: 아이템 하나 고르고 `[조합하기]` → 다른 아이템 클릭 (예: SD카드 + 카메라)
- **자료 뷰어**: 서류·사진은 전체화면으로 펼쳐 여러 장 넘겨본다
- 아이템 정의는 `ItemData.csv`, 조합 규칙은 `ItemCombinations.csv`

### 5. 조사기록 / 메모장 (Note 탭)

- 게임 시작 시점부터 **회사 관련 기본 메모 7줄**이 적혀 있다
- **조사할 때마다 실시간으로 쌓인다.** CSV에 공들여 쓴 문장이 있으면 그것을,
  없으면 조사할 때 나온 내용을 그대로 적는다 → **CSV가 비어 있어도 동작**
- `#07`(회사 잠입) 구간은 실시간 갱신을 멈추고 구간이 끝날 때 한꺼번에 반영
  (CSV의 `NoteRealtime` = `off`/`on`으로 제어)

### 6. 추리 파트

- `DeductionData.csv`에 문제·보기·정답을 적으면 여러 문제를 연속으로 낸다
- **하나라도 틀리면 그 자리에서 배드엔딩**(기본 `Bad_D`)
- 현재 4문제 작성됨 (사인 → 핏자국 → 목격자 → 배후)

### 7. 세이브

- 슬롯 **9개** (세이브포인트 수와 동일)
- **세이브포인트를 지날 때마다 슬롯 선택 창**이 뜬다. "저장하지 않고 계속"도 가능
- 저장되는 것은 "마지막으로 지나온 세이브포인트" → 불러오면 항상 안전한 지점에서 재개
- 진행 위치 + 가방 + 조사기록 + 플레이 시간이 함께 저장된다

세이브포인트 9곳: `#01 사무실` / `#02 해안도로` / `#03 경찰서` / `#04 한성의 방` /
`#회상05 단서 조합` / `#05 공사장` / `#07 회사 조사` / `#07 자료실` / `#07 사장실`

### 8. 엔딩 분기

`#07` 최종 선택지 4개가 각 엔딩 CSV로 연결된다.

| 선택지 | 엔딩 |
|---|---|
| 차장에게 복수한다 | `Bad_B` |
| 사장에게 복수한다 | `Bad_A` |
| 아무것도 하지 않는다 | `Bad_D` |
| 내가 해야만 했던, 지금 해야만 하는 일이... | `True` |

엔딩 대본 끝의 `[END]`를 누르면 진행 상태를 정리하고 타이틀로 돌아간다.

### 9. 환경설정

씬을 오가지 않고 게임 화면 위에 패널로 뜬다. 타이틀과 인게임이 **같은 화면**을 쓴다.

```
        환경설정
[ 사운드 ] [ 화면 ] [ 텍스트 ]
──────────────────────────────
  전체 소리   [═══○──]   70%
  배경음악    [═════○]   90%
  효과음      [══○───]   50%
──────────────────────────────
[메인 화면으로]  [돌아가기] [종료하기]
```

- **사운드**: 전체 / 배경음악 / 효과음
- **화면**: 창 모드 (전체화면에서 4:3 유지, 남는 공간은 검은 여백)
- **텍스트**: 글꼴 4종 / 글씨 크기 / 미리보기 / 텍스트 속도 / 자동 진행 / 대기시간

---

## 고친 버그

| 버그 | 원인 |
|---|---|
| BGM·효과음 슬라이더가 아무 효과 없음 | 값을 실제 소리에 적용하는 코드가 아예 없었음 |
| 엔딩 선택지를 골라도 아무 일 없음 | `TriggerEnding`이 로그만 찍고 있었음 |
| 세이브를 불러와도 처음부터 시작 | 진행 위치를 저장·복원하지 않았음 |
| `#05` 엔딩 연결이 뒤바뀜 | "일상 복귀"가 방화 엔딩으로, "복수"가 도주 엔딩으로 연결돼 있었음 |
| 조사 결과가 팝업으로 뜸 | 이 게임은 대화창에 출력하는 방식 |
| 대사가 대화창을 넘침 | 쪽 나누기가 없었음 |
| 페이드 시 UI까지 사라짐 | 암전 판이 대화창보다 앞에 그려짐 |
| 한글이 안 보임 / 검은 네모 | 코드로 만든 글자가 한글 없는 기본 폰트를 씀 |
| 옵션 창이 게임 뒤로 넘어감 | 씬을 겹쳐 띄우는 구조 문제 → 패널 방식으로 전환 |
| 경고 800개 | 폰트 변환 실패를 캐시하지 않아 매번 재시도 (431건) 외 |
| Account API 30초 경고 | 쓰지 않는 Unity 버전관리 패키지가 계정 인증 시도 |
| 매니저 없으면 게임 정지 | null 검사 없이 호출하던 4곳에 가드 추가 |

---

## 파일 목록

### 새로 만든 스크립트 (17개)

| 파일 | 역할 |
|---|---|
| `Common/IllustLoader.cs` | 그림을 파일명으로 로드 + 캐시 |
| `Common/IllustLayout.cs` | 잘린 그림의 화면 좌표 관리 |
| `Common/UIFontHelper.cs` | 한글 되는 글꼴 자동 판별 |
| `Common/GameBootstrap.cs` | 매니저 자동 생성 |
| `Dialogue/StageController.cs` | 배경 + 스탠딩 무대 |
| `Dialogue/StandingSlot.cs` | 캐릭터 한 자리 + 립싱크 |
| `Dialogue/DeductionController.cs` | 추리 파트 |
| `Save/SavePointManager.cs` | 세이브포인트 규칙 |
| `Save/SaveSlotDialog.cs` | 슬롯 선택 창 |
| `UI/ItemDatabase.cs` | 아이템 정의 로드 |
| `UI/InventoryPanelUI.cs` | 가방 화면 |
| `UI/NoteManager.cs` | 조사기록 관리 |
| `UI/NotePanelUI.cs` | 수첩 화면 |
| `UI/DocumentViewerController.cs` | 서류·사진 뷰어 |
| `UI/SettingsPanelUI.cs` | 환경설정 화면 |
| `UI/AudioManager.cs` | 볼륨 적용 |
| `UI/FontManager.cs` | 글꼴·크기 |
| `UI/AspectRatioKeeper.cs` | 4:3 고정 + 검은 여백 |
| `Editor/IllustTextureImporter.cs` | 그림 임포트 설정 자동화 |
| `Editor/IllustPlacementWindow.cs` | 일러스트 배치 도구 |

### CSV (`Assets/Resources/Dialogues/`)

| 파일 | 내용 | 상태 |
|---|---|---|
| `scenario_01/02/02b/03.csv` | 대사 대본 | 무대·세이브포인트·조사 컬럼 추가 (**대사 원문 보존**) |
| `scenario_ending_*.csv` | 엔딩 6종 | 그대로 |
| `InvestigationData.csv` | 조사 화면 | 전면 재작성 (배경 34개 = 조사 화면) |
| `ItemData.csv` | 아이템 19종 | 신규 |
| `ItemCombinations.csv` | 조합 규칙 | 신규 |
| `NoteEntries.csv` | 조사기록 32항목 | 신규 |
| `DeductionData.csv` | 추리 4문제 | 신규 |
| `Characters.csv` | 화자 ↔ 스탠딩 매핑 | 신규 |
| `IllustLayout.csv` | 그림 배치 좌표 | 배치 도구가 저장 |

> ⚠️ 이 CSV들은 gitignore 대상이라 **git에 올라가지 않는다.** 드라이브로 공유할 것.

---

## 남은 일

### 팀원 담당 (손대지 않음)
- 타임어택 / 이상한 곳 조사 시 조사시간 감소
- 눈치게임 선택지 / 자료실 앞 미니게임

→ `MinigameController`가 스텁으로 자리를 잡아두었다. `LineType=Minigame` 행에서 호출되고,
성공하면 다음 대사로, 실패하면 엔딩으로 가는 배관은 이미 완성되어 있다.

### 아트 담당에게 부탁하면 좋은 것

조사 오브젝트·스탠딩을 **1440x1080 캔버스 크기 그대로**(여백 자르지 말고) 내보내주면
화면 위치가 자동으로 맞는다. 지금은 여백이 잘려 있어서 배치 좌표를 따로 잡아야 한다
(`[2KH1] → [일러스트 배치 도구]`로 105개를 손으로 맞춰야 함).
클립스튜디오/포토샵에서 "표시 범위 자르기"만 끄면 되는 1분짜리 작업이다.

### 검증 상태

CSV 17개 파싱 + 배경·오브젝트·스탠딩·아이템·조사화면 참조 무결성 전체 통과.
컴파일 오류 없음. 경고는 800여 개에서 정리 완료.
